// Placeholder asset for the nn ask feedback surface. Replaced by the built
// Excalidraw React bundle in a later phase. Present now so the Go static-serving
// route has a non-index embedded asset to serve.
console.log("nn ask placeholder");
